## list of directories which contain things we might want to play
root = ["/path/to/media/files"]

## HTTP listener port
port = 18080

## use libavcodec to parse ID3 V2 tags (for possible values see "mplayer -audio-demuxer help")
demuxer = "lavf"